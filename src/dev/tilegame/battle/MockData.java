package dev.tilegame.battle;

import dev.tilegame.gfx.Assets;

public class MockData
{
	public MockData()
	{
		// Temp
		//BattleEngine engine = new BattleEngine();
	}
	
	public static void getAllies()
	{
		//BattleUnit ally = new BattleUnit();
		//battle.addUnit(ally, 1);
		// Note: We need to put this/these battle allies into the main engine
		// Note: Player should have Assets.battleCharacter2Idle for the idle anim
	}
	
	public static void getEnemies()
	{
		//BattleUnit enemy = new BattleUnit();
		//battle.addUnit(enemy, 2);
	}
}